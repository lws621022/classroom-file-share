# 實測報告

測試日期：2026-09-09。Windows 11 x64、Python 3.13.3、Flask 3.1.2、Waitress 3.0.2、PyInstaller 6.15.0。

## 自動化測試

命令：`.venv\Scripts\python.exe -m unittest discover -s tests -v`

結果：11 組測試全部通過（最後一次約 5.4 秒，無警告）。每組內含多個檢查。

| 檢查 | 結果 |
| --- | --- |
| Flask 首頁、UTF-8、打包前靜態資源 | 通過 |
| 空下載目錄及檔案修改日期排序 | 通過 |
| 英文、中文、空白、括號、#、&、+ 檔名下載及內容比對 | 通過 |
| 單檔、多檔、中文檔名上傳及內容比對 | 通過 |
| 同名追加後綴，保留原檔與副檔名 | 通過 |
| 12 個並行同名上傳，全部名稱與內容保留 | 通過 |
| 空白、太短、太長、英文、全形數字學號 | 正確拒絕 |
| ../、URL 編碼、反斜線、絕對路徑、Windows ADS 攻擊 | 正確拒絕 |
| 不存在檔案、上傳目錄列舉、上傳檔案下載、DELETE | 正確拒絕 |
| 指向目錄外檔案的硬連結 | 不列出且不能下載 |
| 超量上傳與部分檔名失敗 | 正確回報，服務持續運作 |
| 設定缺少、損壞、非物件 JSON、目錄不存在 | 正確回報 |
| 設定儲存後重新讀取、建立目錄、目錄重疊防護 | 通過 |
| 實際 localhost HTTP、服務停止及重新啟動 | 通過 |
| Port 已被使用 | 正確拒絕第二個服務 |
| tkinter 真實按鈕 invoke、排程回呼、複製網址及停止 | 通過，HTTP 執行時 GUI 仍回應 |

容量測試將設定降為 1 MB，送入兩個 600,000 byte 檔案及一個更大檔案，驗證總量限制和 HTTP 413；並未實際傳輸 500 MB。

## 打包成品與瀏覽器

- 已產生 `dist/教室檔案分享.exe`，約 15.1 MiB，使用 windowed bootloader。
- 實際開啟 EXE，首次出現繁體中文設定視窗，偵測本機 LAN IPv4。
- 按「儲存設定」建立 EXE 旁的 config.json 與兩個預設資料夾。
- 管理視窗按「啟動服務」顯示執行中；瀏覽器成功載入 EXE 內的 HTML／CSS／JavaScript。
- Chromium 瀏覽器驗證空目錄、重新整理後的中文教材、檔案選擇器一次選兩檔及待上傳清單。
- 實際上傳 `作業 測試 (一).cpp` 與 `homework.txt`，畫面顯示兩個成功檔名，檔案存入老師目錄。
- 點擊中文教材下載連結，收到瀏覽器下載事件；未捕捉到 JavaScript error。
- 按「停止服務」後狀態變為「服務已停止」，localhost socket 無法再連線。
- 關閉後從不同工作目錄重新開啟同一 EXE，設定完整保留，服務仍維持「尚未啟動」。
- 驗證完成後關閉程式；移除成品目錄內的測試設定與測試教材／作業，成品保留空下載／上傳資料夾，交付時重新進行首次設定。

## 修正紀錄

Windows 的 socket reuse 預設行為會讓第二個服務綁定相同 Port。已改用 `SO_EXCLUSIVEADDRUSE` 並通過衝突、停止與重啟測試。測試工具本身的 multipart 暫存 request body 也已明確關閉，最後測試無 ResourceWarning。

## 尚未驗證的環境與限制

- 沒有四間教室與第二台實體電腦，因此未驗證各教室的防火牆、VLAN、學生端實際跨機 LAN 連線。
- 未在未安裝 Python 的乾淨 Windows 虛擬機、Windows 10、USB 或 FAT/exFAT 上實測；本次 EXE 包含 Python／Tcl/Tk 執行資源，在 Windows 11 x64 成功啟動。
- 拖曳事件處理已實作；此次瀏覽器實測使用多檔選擇器，未做 Windows 檔案總管到瀏覽器的實體拖曳操作。
- 尚未進行全班長時間壓力測試、實際磁碟耗盡、突然斷電／拔除磁碟測試。
- 未提供數位簽章。沒有帳號驗證與 HTTPS，適用於題目指定的可信任教室 LAN。

目前已測功能沒有未修復的失敗案例。以上未驗證項目不應視為已通過。

## 公開版本整理驗證

2026-09-09：公開文件及測試用 IP 整理後，11 組測試再次全數通過（約 5.6 秒）。本次未修改應用程式邏輯、未重建正式 EXE。

Release ZIP 通過 CRC 與明確檔案清單檢查；ZIP 內 EXE 與整理前的原檔 SHA-256 一致。23 個待提交來源檔案及 EXE 封裝內的已知私人資訊標記檢查未發現問題；設定、測試資料與建置產物排除於 Git。完整分類見 `docs/PUBLICATION_AUDIT.md`。
